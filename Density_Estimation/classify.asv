function x_class = classify(x, class, p_class)
%associated value to a point x given a class i, for each class
px_new = zeros(1, lenght(class)); %will be proportional to the posteriori probability
for i=1:length(class)
    px = exp(-1/2 * (x-m(i)) * inv(v(i)) * (x-m(i))') / ((2 * pi)^(lenght(x)/2) * det(v(i))^(1/2));
    px_new(i) = px*p_class(i); %we dont need to divide by p(x), we just need to choose the highest value to px_new given by this formula
end

x_class = class(max(px_new)
end

